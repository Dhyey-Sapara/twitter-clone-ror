<% if following?(user) %>
  <%= link_to 'Unfollow', user_unfollow_path(id: user.id), data: { turbo_method: :delete }, class: "btn green-btn" %>
<% else %>
  <%= link_to 'Follow', user_follow_path(id: user.id), data: { turbo_method: :post }, class: "btn green-btn" %>
<% end %>
